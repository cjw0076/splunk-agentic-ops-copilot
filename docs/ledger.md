# Ledger

- 2026-06-05: Workspace created. Initial direction: incident root-cause copilot.
- 2026-06-05: Command packet issued: `docs/packets/SPL-001-command-packet.md`. next: verify Splunk trial/developer license/MCP access.
- 2026-06-09: Built the full working synthetic copilot (credential-free, stdlib-only). Live Splunk stays blocked (reCAPTCHA-gated trial, no `SPLUNK_TOKEN`), so the search backend is the only synthetic part; live is a one-function swap. Delivered: real SPL engine (`src/splunk_copilot/spl.py`), `events.py`, MITRE-mapped correlation `detections.py`, agentic loop `agent.py` (anomaly → wrong hypothesis → contradiction → self-correct → findings → root cause → blast radius → remediation → verify), append-only `ledger.py` + `trace.py`, CLI `__main__.py` (run / `--replay` / `--spl`). Synthetic incident `data/synthetic/incident-01/` (leaked API key → credential stuffing → priv-esc → data exfil) with `ground_truth.json` + SSH-scanner decoy. Tests `tests/test_copilot.py` 19/19 green. Result: recall=precision=1.0, decoy rejected, blast radius correct. Artifacts: `out/ledger.json` (10 records), `out/trace.json` (31 steps). Remaining (founder): Devpost register + repo push + ~3min video.
